# Acoustic Drone Detection System — Python Dependencies
# Install all at once: pip install -r requirements.txt

flask>=2.3.0
scikit-learn>=1.3.0
joblib>=1.3.0
numpy>=1.24.0

# REQUIRED for correct predictions — model was trained with librosa
# Do NOT skip this. scipy alone gives wrong feature scale → wrong predictions.
librosa>=0.10.0

# librosa dependencies (usually auto-installed, listed for reference)
# soundfile
# audioread
# numba
# resampy
gunicorn>=21.2.0
